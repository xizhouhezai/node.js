/**
 * Constants
 */

const StorageKey = {
  /**
   * 用户信息
   * 数据结构为：
   * {
   *  id: 用户id,
   *  token: jwt token,
   *  isVip: 是否vip,
   * }
   */
  USER: 'user',
};

const StaticUrl = 'http://orks6qu1s.bkt.clouddn.com';

module.exports = {
  StorageKey,
  StaticUrl
};
